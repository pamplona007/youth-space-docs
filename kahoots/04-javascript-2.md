# Kahoot — JavaScript 2 (Módulo 3)

> 25 perguntas alinhadas ao conteúdo de 6 aulas (24h total)

---

## Kahoot 1: Arrays e Métodos (Aula 1-2)

**P1.** Qual é a posição do primeiro elemento em um array JavaScript?
- A) `1`
- B) `0` ✅
- C) `-1`
- D) `first`

**P2.** Qual método adiciona um elemento no FINAL do array?
- A) `unshift()`
- B) `push()` ✅
- C) `pop()`
- D) `shift()`

**P3.** Qual método remove o ÚLTIMO elemento de um array?
- A) `push()`
- B) `pop()` ✅
- C) `shift()`
- D) `remove()`

**P4.** Qual método transforma cada elemento de um array?
- A) `filter()`
- B) `reduce()`
- C) `forEach()`
- D) `map()` ✅

**P5.** Qual método filtra elementos de um array?
- A) `map()`
- B) `filter()` ✅
- C) `find()`
- D) `sort()`

**P6.** Qual método reduz um array a um único valor?
- A) `map()`
- B) `filter()`
- C) `reduce()` ✅
- D) `forEach()`

**P7.** Qual é o resultado de `[1, 2, 3].find(x => x > 1)`?
- A) `[2]`
- B) `2` ✅
- C) `true`
- D) `[1, 2]`

**P8.** O que `spread operator` (...) faz em `[...arr1, ...arr2]`?
- A) Copia o array
- B) Expande os arrays em um novo array ✅
- C) Remove elementos
- D) Ordena o array

**P9.** Qual método ordena um array?
- A) `order()`
- B) `sort()` ✅
- C) `arrange()`
- D) ` organize()`

**P10.** Como acessar o terceiro elemento de um array `arr`?
- A) `arr[2]` ✅
- B) `arr[3]`
- C) `arr(2)`
- D) `arr.3`

---

## Kahoot 2: Objetos e JSON (Aula 3)

**P1.** Como acessar a propriedade `nome` de um objeto `pessoa`?
- A) `pessoa->nome`
- B) `pessoa[nome]`
- C) `pessoa.nome` ✅
- D) `pessoa::nome`

**P2.** O que `JSON.stringify()` faz?
- A) Transforma string em objeto JavaScript
- B) Transforma objeto JavaScript em string JSON ✅
- C) Valida JSON
- D) Limpa JSON

**P3.** O que `JSON.parse()` faz?
- A) Transforma string em objeto JavaScript ✅
- B) Transforma objeto em string
- C) Cria um novo objeto
- D) Valida JSON

**P4.** O que é destructuring de objetos em JavaScript?
- A) Destruir um objeto
- B) Extrair propriedades em variáveis ✅
- C) Criar um novo objeto
- D) Deletar propriedades

**P5.** Qual é a sintaxe correta de destructuring?
- A) `{ nome, idade } = pessoa`
- B) `const { nome, idade } = pessoa` ✅
- C) `const [nome, idade] = pessoa`
- D) `const nome, idade = pessoa`

**P6.** O que `this` referencia dentro de um método de objeto?
- A) A função atual
- B) O objeto que contém o método ✅
- C) O escopo global
- D) O navegador

---

## Kahoot 3: DOM e Eventos (Aula 4-5)

**P1.** Qual método seleciona um elemento pelo ID?
- A) `document.querySelector()`
- B) `document.getElementById()` ✅
- C) `document.getElement()`
- D) `document.select()`

**P2.** Qual método seleciona elementos por classe?
- A) `getElementByClass()`
- B) `getElementsByClassName()` ✅
- C) `querySelectorAll()` apenas
- D) `selectAll()`

**P3.** O que `e.preventDefault()` faz em um event handler?
- A) Para o evento completamente
- B) Impede o comportamento padrão ✅
- C) Encerra a função
- D) Impede propagação

**P4.** Qual evento dispara quando um formulário é enviado?
- A) `onClick`
- B) `onChange`
- C) `onSubmit` ✅
- D) `onSend`

**P5.** Como adicionar uma classe "active" a um elemento `el`?
- A) `el.className = "active"`
- B) `el.classList.add("active")` ✅
- C) `el.style.addClass("active")`
- D) `el.addClass("active")`

**P6.** O que `.innerHTML` faz?
- A) Define apenas texto
- B) Define HTML interno ✅
- C) Lê o elemento pai
- D) Remove o elemento

**P7.** Qual método cria um novo elemento DOM?
- A) `document.newElement()`
- B) `document.createElement()` ✅
- C) `document.addElement()`
- D) `document.makeElement()`

**P8.** Para que serve `addEventListener`?
- A) Remover elementos
- B) Escutar e responder a eventos ✅
- C) Criar animações
- D) Selecionar elementos

---

## Kahoot 4: Projeto To-Do e LocalStorage (Aula 6)

**P1.** Para que serve o localStorage?
- A) Armazenar dados no servidor
- B) Persistir dados no navegador ✅
- C) Enviar dados para API
- D) Criar banco de dados

**P2.** Qual método salva dados no localStorage?
- A) `localStorage.set()`
- B) `localStorage.setItem()` ✅
- C) `localStorage.save()`
- D) `localStorage.store()`

**P3.** Como recuperar um item do localStorage?
- A) `localStorage.get()`
- B) `localStorage.getItem()` ✅
- C) `localStorage.retrieve()`
- D) `localStorage.read()`

**P4.** Qual o formato de dados armazenado no localStorage?
- A) Objetos JavaScript
- B) Strings apenas ✅
- C) Números
- D) Arrays

---

**Total: 27 perguntas em 4 Kahoot(s)**
